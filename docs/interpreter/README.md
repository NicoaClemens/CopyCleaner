# Interpreter cpp docs

## Error

## CLI

### CLI Arguments

| Arg | Alias | Effect |
| --- | ----- | ------ |
| --no-alerts | -a | silently disables alerts |
| --no-logs | -l | silently disables logging |
| --no-console | -c | disables console (=> silently disables console log) |

