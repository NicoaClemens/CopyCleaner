# TODOS

note: finished todos should be removed in most cases, not checked off, so the TODOS file doesn't become too large and unreadable.

## dev-related

- [ ] plan helper tool that allows to easily call scripts on shortcuts or per taskbar or whatever
- [ ] fully rework documentation (`/docs`)
---

- [ ] implement alerts for macos, linux
- [ ] fstring doesn't work -> document properly exactly how fstring works first
- [ ] rename alert functions